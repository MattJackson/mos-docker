BaseSystem.img
BaseSystem.dmg
BaseSystem.chunklist
OpenCore.qcow2
mac_hdd_ng.img
*.img
*.qcow2
builds/
mos15.img
.env
kexts/*/build/
kexts/CPUFriend/Tools/CPUFriendDataProvider.kext
*.o
kexts/deps/
kexts/*/build/
oc-check.bin

# Per-deployment runtime artifacts staged by ./setup.sh. Operator-supplied,
# large (disk.img up to ~256 GB, recovery.img ~3 GB, opencore.img ~512 MB).
# Staging instructions live in volumes/README.md.
volumes/*.img
volumes/*.qcow2
# Operator-local boot outputs — per-boot serial logs + QEMU HMP/QMP sockets.
logs/
run/

# Private working notes — live in private gitea (pq/mos)
.claude/
DISCOVERY.md
